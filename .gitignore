## macOS
.DS_Store

## Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

## Xcode workspace / project local state
*.xcworkspace/xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/

## Swift Package Manager
.build/
.swiftpm/
# Package.resolved is committed on purpose: Xcode Cloud disables automatic resolution and requires
# it, and pinning the resolved commits is what makes CI build the same graph we do.


## Handoff archive
wordhop-handoff.zip

# Local secrets (never commit)
.gemini-key

# Rebrand: raw generations and PNG masters stay local (regenerate or keep in Design backups)
Design/collage/raw/
Design/collage/world/*-day.png

# Original generated sheets for the friends; the cut assets are committed instead.
Design/friends/raw/
